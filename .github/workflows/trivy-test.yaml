name: Tofu Trivy Tests

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  id-token: write
  actions: write
  contents: read
  pull-requests: write

jobs:
  trivy:
    name: Trivy (${{ matrix.case.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        case:
          - name: "Valid"
            workdir: "test/valid"
            expect_failure: false
          - name: "Findings"
            workdir: "test/scan"
            expect_failure: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy (${{ matrix.case.name }})
        id: tf-action-trivy
        continue-on-error: ${{ matrix.case.expect_failure }}
        uses: ./
        with:
          workdir: ${{ matrix.case.workdir }}
          steps: trivy
          comment-mode: ${{ matrix.case.expect_failure && 'off' || 'sticky' }}
          summary-mode: full
          skip-plan-upload: true

      - name: Verify Trivy result
        run: |
          if [ "${{ matrix.case.expect_failure }}" == "true" ]; then
            if [ "${{ steps.tf-action-trivy.outcome }}" == "success" ]; then
              echo "❌ Expected Trivy to fail for ${{ matrix.case.name }}"
              exit 1
            else
              echo "✅ Trivy failed as expected for ${{ matrix.case.name }}"
            fi
          else
            if [ "${{ steps.tf-action-trivy.outcome }}" != "success" ]; then
              echo "❌ Expected Trivy to pass for ${{ matrix.case.name }}"
              exit 1
            else
              echo "✅ Trivy passed for ${{ matrix.case.name }}"
            fi
          fi

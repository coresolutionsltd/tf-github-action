name: Tofu PR Comment Test

on:
  pull_request:

permissions:
  id-token: write
  actions: write
  contents: read
  pull-requests: write

jobs:
  pr-comment-test:
    name: PR Comment Test (Validate/Plan/Apply)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run action with PR comments
        uses: ./
        with:
          workdir: test/valid
          env: pr-comment-test
          steps: validate,plan,apply
          summary-mode: redacted
          skip-plan-upload: true
